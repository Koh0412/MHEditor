import React from 'react';

class Infomation extends React.Component {
  constructor(props: {}) {
    super(props);
  }

  render(): JSX.Element {
    return (
      <div>info</div>
    );
  }
}

export default Infomation;
